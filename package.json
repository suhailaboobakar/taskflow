{
  "name": "taskflow",
  "version": "0.1.0",
  "private": true,
  "description": "A premium task management platform built with React, NestJS, Prisma, and PostgreSQL.",
  "engines": {
    "node": ">=22.0.0",
    "npm": ">=10.0.0"
  },
  "workspaces": [
    "apps/*",
    "packages/*"
  ],
  "scripts": {
    "build": "npm run build -w @taskflow/api && npm run build -w @taskflow/web && node ./scripts/verify-workspace.mjs",
    "db:generate": "npm run db:generate -w @taskflow/api",
    "db:migrate": "npm run db:migrate -w @taskflow/api",
    "db:validate": "npm run db:validate -w @taskflow/api",
    "dev": "npm run dev -w @taskflow/web",
    "lint": "node ./scripts/verify-workspace.mjs",
    "test": "node ./scripts/verify-workspace.mjs",
    "typecheck": "npm run typecheck -w @taskflow/api && npm run typecheck -w @taskflow/web && node ./scripts/verify-workspace.mjs",
    "format": "node ./scripts/verify-workspace.mjs"
  },
  "keywords": [
    "task-management",
    "productivity",
    "react",
    "nestjs",
    "prisma"
  ],
  "license": "UNLICENSED"
}
