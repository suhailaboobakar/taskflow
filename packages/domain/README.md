# Domain Package

Framework-independent domain primitives belong here.

This package should contain stable concepts such as task priority values, recurrence types, and shared value objects once they are needed by both client and server.
