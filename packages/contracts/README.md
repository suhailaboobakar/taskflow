# Contracts Package

API-facing schemas and DTO contracts belong here.

Contracts should use Zod and TypeScript inference so the API and web app agree on validation and response shapes without coupling either side to Prisma models.
