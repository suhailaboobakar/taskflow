# Config Package

Shared configuration belongs here when multiple workspaces need the same behavior.

Examples include TypeScript base options, ESLint composition, environment parsing helpers, and build-time constants.
